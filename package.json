{
  "scripts": {
    "migrate-latest": "npx knex migrate:latest --knexfile backend/knexfile.js",
    "migrate-down": "npx knex migrate:down --knexfile backend/knexfile.js",
    "seed-data": "npx knex seed:run --knexfile backend/knexfile.js",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.17.3",
    "knex": "^2.0.0",
    "pg": "^8.7.3"
  }
}
